# Unified UMCH paper

Authoritative bilingual synthesis of hypothesis history, spacetime restart, operational protocols, exact controls, ell0 non-identifiability theorem, turnover candidate, and shape-derivation gate.

Scientific status: `UNPROVEN`; `NO_POSITIVE_DETECTION_CLAIM`.

Build:

```bash
cd papers/umch/it && pdflatex main.tex && bibtex main && pdflatex main.tex && pdflatex -jobname=umch-unified-it main.tex
cd papers/umch/en && pdflatex main.tex && bibtex main && pdflatex main.tex && pdflatex -jobname=umch-unified-en main.tex
```

Older papers remain historical technical records and provenance, not current authoritative synthesis.
