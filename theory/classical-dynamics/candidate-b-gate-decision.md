# Candidate B — Gate decision

Core UMCH status remains `UNPROVEN`.

## Decision

Fixed barrier state: `CONTRADICTED_UNDER_ASSUMPTIONS`.  
Paper III gate: `BLOCKED`.

This is conditional rejection of exact flat-spacetime timelike pointwise barrier

\[
L=mc\left[-1+\epsilon/(\kappa/\kappa_0-1)\right],
\quad \epsilon>0,\ \kappa>\kappa_0.
\]

It is not a full disproof of UMCH, Candidate A, or separately ratified alternatives.

## Evidence passed

- covariant first-curvature variation exists in sourced generic `L(κ)` sector;
- `L_{κκ}>0` gives invertible curvature Legendre map;
- source generic constraint chain closes with primary/secondary two first-class constraints;
- unique planar constant-curvature stationary point exists.

## Failed required checks

### Reduced local stability

At stationary point `z*=1+ε+√(ε²+ε)`, local planar equation is

\[
\delta z''-(\kappa_0z_*)^2\delta z=0.
\]

It has a growing exponential. This is a local planar reduced failure, not a full constrained phase-space instability theorem. Nevertheless, preregistered viability required stable linearized behavior around a simple admitted background; fixed barrier fails that criterion.

### Standard limit

Limit is nonuniform:

- fixed positive curvature: barrier vanishes pointwise;
- stationary/boundary-layer curvature proportional to `κ₀`: finite path-dependent barrier remains;
- exact geodesic: outside domain for every positive `κ₀`.

Stationary growth rate tends to zero with `κ₀`, so timescale diverges, but full standard solution space is not recovered for any positive parameter.

### Observable

No experiment-to-`κ₀` mapping follows from canonical structure. Curve exclusion is not quantitative observable.

## Unresolved, not fabricated

Full reduced Hamiltonian boundedness and nonlinear/full constrained stability remain unresolved. Decision does not depend on claiming either: candidate already fails three required gates under fixed assumptions.

## Consequence

Paper III cannot use Candidate B as foundation. To resolve overall pointwise UMCH block, next valid route is complete Candidate A nonsmooth dynamics or conditional no-go covering a stated class. Changing barrier would be a new preregistered candidate, not a repair of B.
