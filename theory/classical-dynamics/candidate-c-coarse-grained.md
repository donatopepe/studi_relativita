# Candidate C — Coarse-grained curvature bound

Scientific status: `UNPROVEN`  
Hypothesis class: `ALTERNATIVE_HYPOTHESIS`

## Domain

Timelike histories plus a declared averaging interval or kernel. Instantaneous `κ=0` is permitted.

## Candidate functional or condition

Illustrative condition only:

\[
\sqrt{\langle\kappa^2\rangle_{\Delta\tau}}\ge\kappa_0.
\]

Averaging measure, window, endpoint behavior, state dependence, and operational estimator are unspecified.

## Analysis level

`CONJECTURAL`. This is not equivalent to pointwise UMCH and cannot rescue Candidates A or B by relabeling.

## Differential order

Depends on eventual dynamics; condition alone does not define equations. Nonlocal windows may require history data.

## Constraints

No constraint algebra defined. Covariance under reparametrization requires proper-time or an invariant kernel.

## Standard limit

At `κ₀→0`, inequality becomes nonrestrictive if averaging remains finite. Dynamics must independently recover standard theory.

## Observable

Potentially an RMS proper-acceleration estimator over a calibrated window; no experimental mapping or uncertainty model exists.

## Blocking issue

Need operational averaging protocol and independent scientific ratification as a renamed hypothesis.
