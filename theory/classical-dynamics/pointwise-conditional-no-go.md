# Conditional pointwise no-go for fixed Candidates A and B

Core `κ₀>0` remains `UNPROVEN`.

## Result

`CONDITIONAL_POINTWISE_NO_GO_FOR_FIXED_A_AND_B` under timelike pointwise definition.

- Candidate A fixed normalized hard constraint: `CONTRADICTED_UNDER_ASSUMPTIONS`.
- Candidate B fixed barrier: `CONTRADICTED_UNDER_ASSUMPTIONS`.
- Paper III gate: `BLOCKED`.

## Candidate A failure basis

Inactive smooth branch has λ=0 and free action, whose geodesic solution κ=0 is infeasible for each κ₀>0. Active branch is linear-curvature degenerate and requires coupled multiplier dynamics. Active-set jumps imply `DISTRIBUTIONAL_MATCHING_REQUIRED`, but matching, global existence, uniqueness, and continuous dependence are not derived.

Normalized action contains λ/κ₀. At `κ₀→0`, fixed λ is singular; scaling λ changes reaction force and needs a derived solution family. Feasible sets include geodesics only exactly at zero, not for positive parameter. No observable mapping exists.

## Candidate B prior result

Fixed smooth barrier has sourced canonical closure, but fails reduced planar local-stability, full standard-solution-space limit, and observable gates.

## Scope

Conditional result does not exclude:

- other preregistered actions or barriers;
- nonlocal or stochastic models;
- null, field, or vacuum sectors;
- Candidate C, which is a separately ratified `ALTERNATIVE_HYPOTHESIS` and not equivalent;
- every mathematical realization of UMCH.

It blocks ALD/Paper III from using either analyzed pointwise mechanism as foundation.

## Epistemic limit

No global theorem classifying all inequality-constrained worldline actions is claimed. Candidate A rejection follows preregistered viability gates: absent matching/well-posedness, geodesic incompatibility, singular standard-limit structure, and absent observable.
