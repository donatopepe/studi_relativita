# Exact plane-wave window/Jacobi cross-channel map

Classification: `EXACT_SPACETIME_CROSS_CHANNEL_CONTROL_AND_NEGATIVE_IDENTIFIABILITY_RESULT`.

Status: `EXACT_PLANE_WAVE_WINDOW_JACOBI_MAP_CONDITIONAL_SUPPORT_IDENTIFIABILITY_NOT_ELL0`; `NO_POSITIVE_DETECTION_CLAIM`.

For the same canonical-sourced vacuum Brinkmann plane-wave profile `K(u)`, parallel screen and affine interval, derive two channel-native objects:

`W(L)=int_{-L/2}^{L/2}K(u)du`,

and vertex Jacobi block `B(L)=D(L/2)` from

`D''+K(u)D=0`, `D(-L/2)=0`, `D'(-L/2)=I`.

This is a physical mathematical cross-channel dependence: both channels share curvature history rather than an arbitrary fitted map. But `B` is not determined by unweighted `W`. An exact numerical counterexample adds an odd tracefree perturbation whose centered integral vanishes: `W` agrees while nonlinear ordered Jacobi propagation yields distinct `B`. Changing vertex displacement boundary also changes `B` without changing `W`.

With profile, affine normalization, boundary and absolute channel calibration fixed, the joint curve changes noncollinearly with support width in a bounded local control. This is only conditional support-width information. Independent scalar projection gains can match two widths exactly, and affine transformation

`L2=s L1`, `K2(u)=K1(u/s)/s^2`

preserves dimensionless channels `L W` and `B/L`. Thus profile frequency/amplitude and affine normalization carry an exact rescaling nuisance.

Free-gain equivalence is explicitly scoped to scalar norm projections, not equality of raw matrices. Raw objects remain primary. No `ell0` occurs; identifying affine support width conditional on known geometry is not identifying a universal scale. Detector calibration, physical window selection and a law linking geometry to `ell/ell0` remain absent. No structural dead end or reformulation follows.
