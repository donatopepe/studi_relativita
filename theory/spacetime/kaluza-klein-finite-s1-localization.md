# Finite localization on the compact circle

## Classification

- `KNOWN_RESULT`: `NISTDLMF` Poisson summation (1.8.14) and theta-3 Fourier series (20.2.3); prior compact-circle KK potential sources remain scoped.
- `PROJECT_DERIVATION`: normalized wrapped Gaussian, raw complex source/probe Fourier coefficients, their combined overlap, static real mode weights, point Hessian, radial-shell matrix, scaling and rank.
- `TOY_CONTROL`: static scalar weak-field compact circle with mathematical preparation labels.
- `NEGATIVE_RESULT`: static response depends on combined width, is even and periodic in separation, and retains absolute-scale null direction.
- `OPEN_PROBLEM`: physical localization dynamics, gauge-fixed tensor coupling, radion stabilization, phase-sensitive receiver, calibrated covariance, data and `ell0` law.
- `HYPOTHESIS`: observed gravity could be higher-dimensional effective geometry; passing controls is not evidence.

## Raw complex and static records

The wrapped profile is normalized by its image sum. Poisson summation yields Gaussian-damped coefficients. Source and probe coefficients remain raw complex preparation records. Their combined overlap is conjugated under orientation reversal or source–probe exchange. Only its real projection enters the present static scalar potential and tidal matrix.

Consequently complex labels may distinguish sign while static real response cannot. No physical phase-sensitive channel is derived.

## Exact collisions

Static attenuation depends on

\[
u=(w_s^2+w_p^2)/L^2,
\qquad \theta=\Delta y/L,
\]

through Gaussian damping and `cos(n theta)`. Therefore distinct width pairs with equal `u` collide for every mode, point matrix and radial shell. Static response also collides under `theta -> -theta` and `theta -> theta+2*pi`.

```text
BROAD_WRAPPED_GAUSSIAN_APPROACHES_ZERO_MODE_BUT_FINITE_WIDTH_IS_NOT_EXACT_UNIFORM
S1_RELATIVE_ORIENTATION_SIGN_COLLISION_IN_STATIC_REAL_RESPONSE_NOT_COMPACTIFICATION_SCALE
SOURCE_PROBE_LOCALIZATION_WIDTHS_COLLIDE_UNDER_COMBINED_MODE_OVERLAP
JOINT_5D_LOCALIZATION_GEOMETRIC_DILATION_NOT_ABSOLUTE_SCALE
```

The raw matrix remains primary. Scalar features and rank do not replace it. `F_0` and all prior null/negative controls are preserved.

## Result and gate

```text
FINITE_S1_SOURCE_PROBE_LOCALIZATION_SUPPRESSES_KK_TIDAL_SHAPE_BUT_STATIC_RESPONSE_IDENTIFIES_ONLY_COMBINED_WIDTH_AND_EVEN_PERIODIC_SEPARATION_WHILE_JOINT_DILATION_RETAINS_ABSOLUTE_SCALE_BLINDNESS_NOT_ELL0
```

This model result is not evidence for extra dimensions, localization, `L`, `ell0`, or UMCH.

```text
PHYSICAL_5D_LOCALIZATION_DYNAMICS_GAUGE_FIXED_TENSOR_COUPLING_RADION_STABILIZATION_SOURCE_PROBE_PREPARATION_PHASE_SENSITIVE_RECEIVER_CALIBRATED_NOISE_JOINT_COVARIANCE_DATA_AND_ELL0_LAW_NOT_DERIVED
```
