# Finite-window kernel-scale landmark

Classification: `TOY_CONTROL_AND_NEGATIVE_RESULT`.

Status: `FINITE_WINDOW_SPECTRAL_LANDMARK_KERNEL_DILATION_MOVABLE_NOT_ELL0`; `NO_POSITIVE_DETECTION_CLAIM`.

For synthetic profile `K(s)=diag(1,s^2)`, normalized top-hat average over `[-kappa ell,kappa ell]` is

`R(ell,kappa)=diag(1,kappa^2 ell^2/3)`.

Projective spectrum varies and becomes isotropic at `ell_cross=sqrt(3)/kappa`. Any positive target is produced by kernel dilation `kappa=sqrt(3)/ell_target`. In physical half-width coordinate `L=kappa ell`, same crossing is always `L=sqrt(3)`: reported `ell` landmark depends on scale convention.

Common amplitude preserves crossing, but kernel dilation moves it. If covariant kernel, region family and reported scale definition are independently fixed, crossing characterizes profile/window geometry only. It does not identify `ell0`, absent here, without derived fixed relation.

This is exact integration of synthetic one-dimensional operator profile, not exact spacetime geometry or observation. Covariant exact-geometry kernels remain open; no core reformulation is triggered.
