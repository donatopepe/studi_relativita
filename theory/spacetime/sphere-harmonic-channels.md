# Sphere cross-channel harmonic dependence

Classification: `PROJECT_DERIVATION_AND_NEGATIVE_RESULT`.

Status: `SPHERE_CROSS_CHANNEL_HARMONIC_ALGEBRAIC_DEPENDENCE_NOT_ELL0`; `NO_POSITIVE_DETECTION_CLAIM`.

For exact sphere holonomy phase `alpha=eta ell^2/r^2`, consider synthetic conjugacy-invariant channels

`y1=cos(alpha)`, `y2=cos(2alpha)`.

Chebyshev identity gives `y2=2y1^2-1`. Joint response lies on algebraic curve and has differential rank at most one; second channel adds no independent phase information. Map remains even and `2pi` periodic. On preregistered `[0,pi]` branch, `y1` recovers phase, but globally sign and winding alias.

If first channel is spectral-gap magnitude `g=|cos alpha|`, pair satisfies `y2=2g^2-1` and is `pi` periodic. Thus second invariant cannot restore orientation or branch information erased by gap projection.

A sine channel could encode orientation locally only after oriented frame/loop convention, and still leaves `2pi` winding. No such independent physical channel is derived here. Known `eta`, `r` and branch recover geometric `ell/r`; unknown geometry contains only `eta/r^2`. `ell0` is absent.

This is synthetic cross-channel specialization of exact sphere phase, not physical channel derivation or data. No core reformulation is triggered.
