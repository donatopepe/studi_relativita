# Exact plane-wave screen transport, finite windows, and Jacobi maps

Classification: `EXACT_SPACETIME_TRANSPORT_WINDOW_JACOBI_CONTROL_AND_NEGATIVE_IDENTIFIABILITY_RESULT`.

Status: `EXACT_PLANE_WAVE_SCREEN_TRANSPORT_AVERAGE_ORDER_OPERATOR_AND_JACOBI_PROTOCOL_DEPENDENT_AFFINE_SCALE_BLIND_NOT_ELL0`.

Open gate: `PHYSICAL_SCREEN_CONNECTION_PATH_KERNEL_AND_COMMON_ENDPOINT_STANDARD_NOT_DERIVED`.

## Raw objects

Retain `K`, `omega`, `Q`, `W_raw`, `W_transport`, local invariant averages, `P_raw`, and `P_transport`. For a declared centered screen connection,

`Q'(u)=-omega(u) J Q(u)`, `Q(0)=I`,

`W_raw=integral w_L(u) K(u) du`,

`W_transport=integral w_L(u) Q(u)^T K(u) Q(u) du`.

`P_raw` and `P_transport` solve the full Jacobi phase-space equation with `K` and `Q^T K Q`, respectively. Top-hat and triangular kernels remain explicit.

Zero connection collapses both orderings. A varying connection preserves pointwise trace, determinant, eigenvalues, and Frobenius norm but changes `W_transport` and `P_transport`. Their averages cannot reconstruct transported operator or ordered propagator. Thus transport, averaging, averaging invariants, and Jacobi propagation are not interchangeable.

A common constant screen basis conjugates `W_transport` and canonically conjugates `P_transport`; characteristic coefficients stay fixed. Different `omega` at fixed `K,L,kernel` move outputs, proving protocol dependence.

Under `K_s(u)=s^-2 K(u/s)`, `omega_s(u)=s^-1 omega(u/s)`, and `L_s=sL`, dimensionless `L W_transport` and characteristic coefficients collide. No absolute scale or `ell0` follows.

The chosen `omega` is a project protocol input, not a physical detector derivation. Coley–McNutt–Milson 2012, DOI `10.1088/0264-9381/29/23/235023`, supports exact vacuum Brinkmann plane waves and curvature-driven geodesic deviation. Screen connection, path/anchor, window/kernel, common endpoint standard, affine law, UMCH, `ell0`, and detection are not established by the source.

Physical source/observer tetrads, Fermi/parallel screen transport along a declared path, causal support, common calibration, and other exact geometries remain open. This is not a structural dead end.

UMCH remains `UNPROVEN`; `NO_POSITIVE_DETECTION_CLAIM`.
