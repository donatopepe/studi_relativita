# Kottler finite-boundary null-scattering Jacobi control

For `f(r)=1-2M/r-Lambda*r^2/3`, define `alpha=Lambda*M^2`, `rho=r_min/M`, and finite endpoint `R=r_end/M` inside static patch. Unit Killing energy is a toy normalization. The canonical `b=Phi/E` used here is not an asymptotically measured impact parameter.

`RindlerIshak2007` supports metric, `u''+u=3M*u^2`, and coordinate-path/local-angle distinction only. Project derivation reconstructs four-dimensional Riemann curvature, keeps raw Ricci/null-Ricci/screen records, and propagates full `4x4` Jacobi phase map.

Kottler is Einstein: nonzero spacetime Ricci coexists with zero null Ricci contraction. Thus:

```text
NULL_RICCI_FOCUSING_IN_EINSTEIN_SPACE_NOT_ZERO_SPACETIME_RICCI
```

Lambda cancellation from coordinate orbit does not imply cancellation from static boundaries or normalization. After declared effective-Schwarzschild affine/frequency conversion, bounded path and Jacobi shape cancel; joint `(M,Lambda)` dilation leaves an absolute-scale null direction. Fixed dimensional Lambda is an imported standard, not `ell0`.

Result:

```text
KOTTLER_LAMBDA_ADDS_STATIC_BOUNDARY_NORMALIZATION_BUT_NULL_RICCI_FOCUSING_AND_CONVERTED_NULL_JACOBI_SHAPE_CANCEL_WHILE_JOINT_MLAMBDA_DILATION_RETAINS_ABSOLUTE_SCALE_BLINDNESS_NOT_ELL0
```

Gate:

```text
PHYSICAL_COSMOLOGICAL_MATCHING_SOURCE_EMITTER_ABSORBER_ENDPOINT_SCREEN_PREPARATION_ABSOLUTE_FREQUENCY_RECEIVER_TRANSFER_CALIBRATED_NOISE_JOINT_COVARIANCE_AND_ELL0_LAW_NOT_DERIVED
```

Status remains `UMCH=UNPROVEN`, `ell0_identified=false`, `structural_dead_end=NOT_DECLARED`, `NO_POSITIVE_DETECTION_CLAIM`, maximum `CONFIRMATORY_ANALYSIS_ELIGIBLE_NOT_EVIDENCE`.
