# Reissner–Nordström finite-boundary Jacobi control

For `f(r)=1-2M/r+Q^2/r^2`, neutral equatorial null scattering depends on dimensionless `epsilon=Q/M`, `rho=r_min/M` and `R=r_end/M` after unit-Killing-energy normalization. The project reconstructs the screen optical matrix directly from four-dimensional Riemann curvature and propagates full `4x4` phase map. Graph objects are used only where the congruence block is invertible.

Charge makes optical trace nonzero and changes trace-free profile. Both are parts of one raw curvature projection; without joint covariance they are not independent observations. Because metric depends on `Q^2`, `Q` and `-Q` collide exactly:

```text
Q_SQUARED_METRIC_DEGENERACY_NOT_ELL0
```

At fixed `epsilon,rho,R`, joint scaling `(M,Q,r,b,lambda)->s(M,Q,r,b,lambda)` changes dimensional phase rates but not declared frequency-converted dimensionless record. Thus `Q`, `Q/M`, photon sphere, turning point and impact parameter are geometric/source labels, not `ell0`.

Result:

```text
REISSNER_NORDSTROM_CHARGE_ADDS_DIMENSIONLESS_RICCI_WEYL_OPTICAL_SHAPE_BUT_Q_SQUARED_DEGENERACY_AND_JOINT_MQ_DILATION_RETAIN_ABSOLUTE_SCALE_BLINDNESS_NOT_ELL0
```

Gate:

```text
PHYSICAL_CHARGE_SOURCE_EMITTER_ABSORBER_ENDPOINT_SCREEN_PREPARATION_ABSOLUTE_FREQUENCY_RECEIVER_TRANSFER_CALIBRATED_NOISE_JOINT_COVARIANCE_AND_ELL0_LAW_NOT_DERIVED
```

Current interpretation remains `UMCH=UNPROVEN`, `ell0_identified=false`, `structural_dead_end=NOT_DECLARED`, `NO_POSITIVE_DETECTION_CLAIM`, and at most `CONFIRMATORY_ANALYSIS_ELIGIBLE_NOT_EVIDENCE`.
