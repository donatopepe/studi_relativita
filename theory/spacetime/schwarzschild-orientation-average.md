# Schwarzschild-pattern orientation-cap average

Classification: `EXACT_PATTERN_CONTROL_AND_NEGATIVE_RESULT`.

Status: `SCHWARZSCHILD_CAP_AVERAGE_SIGN_REVERSAL_ORIENTATION_DOMAIN_NOT_ELL0`; `NO_POSITIVE_DETECTION_CLAIM`.

Use normalized radial tidal pattern `E(n)=I-3nn^T`, with Schwarzschild principal spectrum `(-2,1,1)`. Uniform solid-angle average over cap `0<=theta<=Theta` around z-axis gives exactly, with `c=cos Theta`,

`Ebar=diag((c+c^2)/2,(c+c^2)/2,-c-c^2)`.

Below hemisphere (`c>0`), all nonzero averages share signed projective spectrum `(-1,1/2,1/2)`. Above hemisphere (`-1<c<0`), coefficient reverses sign and signed ray is opposite. Hemisphere is zero; full sphere also averages to zero. Thus rotationally symmetric orientation expansion preserves axial pattern and multiplicity within each sign sector, yet creates zero/sign reversal solely from integration domain.

A hemisphere zero or signed-ray crossing is therefore orientation-measure/domain landmark, not automatically curvature scale. Common-space alignment, cap measure and region boundary must be fixed before interpretation. `ell0` is absent.

This is exact angular integration of Schwarzschild algebraic pattern after assumed Euclidean tangent-space alignment, not Schwarzschild parallel transport; radial amplitude variation is omitted. Covariant spacetime region route remains open; no core reformulation is triggered.
