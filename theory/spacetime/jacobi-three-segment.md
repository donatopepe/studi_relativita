# Exact scalar Jacobi three-segment permutation gate

Classification: `EXACT_JACOBI_CONTROL_AND_NEGATIVE_RESULT`.

Status: `JACOBI_THREE_SEGMENT_SPECTRUM_PERMUTATION_BLIND_VERTEX_ENDPOINT_MIDDLE_ONLY_NOT_ELL0`; `NO_POSITIVE_DETECTION_CLAIM`.

For three exact scalar segment propagators `P_i` of `d''+lambda_i d=0`, enumerate all six total products. Test-first algebra shows stronger symmetry than generic matrix expectation: trace and determinant are identical for every permutation, not merely cyclic rotations. Hence total eigenvalues and characteristic polynomial are fully permutation-blind for this scalar Jacobi class.

For generic distinct segments, six full phase-space maps remain distinct. Vertex displacement `M01`, however, forms three pairs: reversing outer segments around same middle segment leaves displacement unchanged. It therefore identifies at most which segment occupies middle position, not orientation of the two outer segments. Observer derivative and full map can retain more information after boundary basis and affine normalization are fixed.

Equal segments collapse every permutation. Special phases can create further endpoint collisions. Thus local segment spectra plus total-map spectrum are insufficient; even vertex displacement preserves a reversal nuisance. Retained order information depends on full phase-space observable, boundary data and screen transport.

Exact piecewise-constant scalar optics only, not smooth matrix Sachs or exact spacetime. `ell0` is absent; ordering measures optical geometry. Matrix-valued varying profiles remain open, so no structural dead end or reformulation is triggered.
