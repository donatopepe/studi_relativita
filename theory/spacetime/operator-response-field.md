# Operator-valued multiscale response field

Status: `RATIFIED_PRIMARY_RESEARCH_OBJECT`; identifiability `NOT_YET_DERIVED`; `NO_POSITIVE_DETECTION_CLAIM`.

Spacetime is an intrinsic four-dimensional Lorentzian manifold, not a curve or required embedding surface. Primary response is a section over region/scale/frame/protocol space:

`mathscr R[Omega,ell,u_phys,P;g] in E_(Omega,ell,u,P)`.

Channel-native fibers include tidal/magnetic endomorphisms, finite-loop holonomy maps, clock-network residual matrices, null Jacobi maps, and congruence fields. Six labels index channel families; they do not assert six scalar independent measurements.

Raw objects are primary. `C_2/C_infinity` and scalar threshold families are descriptive projections. Turnover `F_T` and q remain valid only inside secondary scalar branch.

Potential amplitude-independent invariants include projective direction, eigenvalue ratios, spectral flow, rank/nullity, multiplicity, holonomy conjugacy/singular values, anisotropy, crossings, and caustic/parity structure. Identifying `ell0` requires an invariant map injective in `x=ell/ell0`, or theory-fixed landmark `ell*=alpha ell0`, after nuisance quotient.

Counterexample: if all response objects remain proportional along same ray as ell varies, projective normalization is constant and scale is non-identifiable. A synthetic two-channel model with independently fixed unequal exponents is identifiable in principle, but no physical UMCH channel exponents/coefficients are derived by this toy.
