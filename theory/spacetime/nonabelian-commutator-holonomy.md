# Exact non-Abelian commutator holonomy gate

Classification: `EXACT_GROUP_TOY_CONTROL_AND_NEGATIVE_RESULT`.

Status: `NONABELIAN_COMMUTATOR_TRACE_PRODUCT_DEGENERACY_NOT_ELL0`; `NO_POSITIVE_DETECTION_CLAIM`.

For exact noncommuting `SL(2,R)` shears

`A(a)=[[1,a],[0,1]]`, `B(b)=[[1,0],[b,1]]`,

the group commutator `C=ABA^-1B^-1` has

`det C=1`, `tr C=2+(ab)^2`.

Thus conjugacy-invariant trace identifies at most `|ab|`. It loses product sign and cannot separate factors: `(a,b)` and `(ca,b/c)` collide for every nonzero `c`. Noncommutativity and path ordering therefore do not by themselves supply parameter identification.

With path/window laws `a=kappa ell^p`, `b=rho ell^q`,

`tr C(ell)=2+(kappa rho)^2 ell^(2(p+q))`.

Only coefficient product magnitude and exponent sum enter. For any trace threshold `tau>2`, free product amplitude moves reported crossing to any positive scale. Full matrix retains coordinate information but changes under anchor/frame conjugation; in this `SL(2)` toy determinant is fixed, so trace already fixes characteristic polynomial.

No four-dimensional spacetime connection, physical loop family, calibration, data, or `ell0` law is derived. Exact non-Abelian group toy only. Connection-derived spacetime loops remain open; no structural dead end or core reformulation is triggered.
