# Schwarzschild finite-scattering polarization in a transported screen

Status: `PROJECT_DERIVATION_WITH_TOY_BOUNDARY_CONTROL`; review `DIRECT_REVIEW_NO_SUBAGENT`.

Result: `SCHWARZSCHILD_LEADING_POLARIZATION_IS_CONSTANT_IN_PARALLEL_SCREEN_AND_ENDPOINT_ANALYZER_IS_BASIS_PREPARATION_NUISANCE_RETAINING_GEOMETRIC_SCALE_BLINDNESS_NOT_ELL0`.

Gate: `PHYSICAL_POLARIZATION_SOURCE_STATE_EMISSION_ABSORPTION_ENDPOINT_SCREEN_PREPARATION_POLARIZATION_SENSITIVE_RECEIVER_TRANSFER_CALIBRATED_NOISE_JOINT_COVARIANCE_AND_ELL0_LAW_NOT_DERIVED`.

Authority remains `UMCH=UNPROVEN`, `ell0_identified=false`, `structural_dead_end=NOT_DECLARED`, `NO_POSITIVE_DETECTION_CLAIM`, `CONFIRMATORY_ANALYSIS_ELIGIBLE_NOT_EVIDENCE`.

## Construction

For the corrected finite equatorial Schwarzschild null-scattering screen in order `(polar,in-plane)`, leading geometrical optics parallel-transports polarization. In screen components:

```text
j_s=(cos psi_s,exp(i delta_s) sin psi_s)
U_screen=I_2 modulo the null-gauge direction
j_R=U_screen j_s
J_R=j_R j_R^dagger
R_polarization=(j_R,J_R,Phi_clock,P_frequency_converted)
```

The source Jones state and endpoint analyzer are `TOY_CONTROL`, not derived source polarization or receiver dynamics. The finite endpoint joining and rank audit are `PROJECT_DERIVATION`. `j_R` and `J_R` remain raw vector/matrix entries; analyzer power is secondary. The complete corrected `4x4` `P_frequency_converted` remains primary optical information through caustics and is not replaced by polarization readout.

Raw screen covariant derivatives need not vanish. Their null-gauge quotient and screen rotation residual converge under refinement, which permits component constancy in this declared mathematical screen. This does not derive physical screen preparation. A common SO(2) basis rotation acts jointly on Jones, coherency and analyzer; invariant analyzer amplitude under that joint action is a mathematical basis quotient, not physical calibration or a gravitational Faraday observable.

## Counterexample result

Analyzer angle can change projected power while raw Jones/coherency are unchanged. Arbitrary source polarization can survive the zero-window limit as boundary data. Equatorial orientation reversal is symmetric in the declared screen. Under `M -> sM` at fixed `rho`, `R`, `nu_s=M omega_s` and polarization labels, Jones, coherency, clock phase and full frequency-converted map remain invariant. Raw and basis-quotiented Jacobians over `(rho,R,log M)` retain rank two and scale null direction `[0,0,1]`. Global injectivity is `NOT_ESTABLISHED`; statistical independence is `DEPENDENCE_UNRESOLVED_WITHOUT_JOINT_COVARIANCE`.

Thus polarization adds boundary-state coordinates and possible analyzer shape, not an internally identified scale or `ell0`.

## Limitations and source scope

Missing physical ingredients include source polarization preparation and spectrum, coherence dynamics, emission, absorption, endpoint screen preparation, polarization-sensitive receiver transfer, calibrated noise, joint covariance and a geometry-to-`ell0` law. No source model, material analyzer or detector response is inferred from geometry.

`Schwarzschild2003Translation` is used only for Schwarzschild exterior/static context; `Darwin1959GravityField` only for nonradial null-scattering context; `Sachs1961` only for null optical/Jacobi context; `Dolan2018GeometricalOptics` only for leading geometrical optics, null rays and parallel-propagated polarization. These sources do not establish the project endpoint protocol, physical source polarization, emission/absorption, screen preparation, analyzer hardware, receiver, noise, covariance, `ell0`, UMCH, evidence or detection.
