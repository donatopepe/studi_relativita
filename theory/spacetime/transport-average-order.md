# Transport and finite-window averaging order

Classification: `TOY_CONTROL_AND_NEGATIVE_RESULT`.

Status: `TRANSPORT_AND_WINDOW_AVERAGING_ORDER_NONCOMMUTATIVE`; `NO_POSITIVE_DETECTION_CLAIM`.

Let one anisotropic operator be represented in two orthonormal local frames by `A=diag(3,1)` and `B=QAQ^T=diag(1,3)`. Raw coordinate average is `2I`, creating isotropy and eigenvalue multiplicity. Transporting `B` back to frame of `A` before averaging instead returns `A` and preserves anisotropy.

Hence transport/frame alignment and finite-window averaging do not commute. Raw coordinate averaging can manufacture spectral shape from unchanged physical local operator. Averaging pointwise conjugacy invariants avoids frame mixing but discards relative orientation and connection information. Transport-then-average requires fixed anchor, path family, boundary, region measure and uniqueness certificate.

Average-then-quotient and quotient-then-average are distinct maps. Their scale-dependent difference is protocol dependent, not an `ell0` landmark. `ell0` is absent.

This is 2x2 orthogonal-frame algebra, not a spacetime bitensor integral or observation. Covariant exact-region transport remains open; no core reformulation is triggered.
