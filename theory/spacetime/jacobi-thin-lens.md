# Jacobi thin-lens profile-location gate

Classification: `PROJECT_DERIVATION_AND_NEGATIVE_RESULT`.

Status: `JACOBI_INTEGRATED_FOCUSING_INSUFFICIENT_PROFILE_LOCATION_REQUIRED`; `NO_POSITIVE_DETECTION_CLAIM`.

For scalar distributional equation `d''+k delta(s-a)d=0` with vertex data `d(0)=0,d'(0)=1`, solution before lens is `d=s`. Continuity and derivative jump give

`d'(a+)=1-ka`,

`d(s)=a+(1-ka)(s-a)` for `s>a`.

At observer `S>a`, endpoint is `d(S)=S-k a(S-a)`. Thus fixed integrated optical strength `k` does not determine endpoint: lens location `a`, i.e. ordered profile, matters. If `ka>1`, first post-lens caustic is

`s_c=a+a/(ka-1)`;

otherwise no post-lens positive zero occurs. Same `k` can yield no caustic or different caustics as `a` varies.

Therefore integrated trace/strength is insufficient statistic even in exact scalar thin-lens control. Affine normalization, ordered optical profile, source/observer boundary data and screen transport must be fixed. Result is geometric/path dependent and contains no `ell0`.

This is idealized distributional scalar optics, not smooth exact spacetime or observation. Smooth varying-matrix routes remain open; no core reformulation is triggered.
