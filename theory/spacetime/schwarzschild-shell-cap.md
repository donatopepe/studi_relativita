# Schwarzschild shell-cap finite-window factorization

Classification: `EXACT_PATTERN_WINDOW_CONTROL_AND_NEGATIVE_RESULT`.

Status: `SCHWARZSCHILD_PRODUCT_SHELL_CAP_REMAINS_FACTORABLE_NOT_ELL0`; `NO_POSITIVE_DETECTION_CLAIM`.

Use exact algebraic Schwarzschild tidal field

`E(r,n)=m r^-3 (I-3nn^T)`

with product measure: normalized positive radial density on a shell and uniform angular cap with `c=cos Theta`. Under explicitly assumed common tangent-space alignment, radial and angular integrations factor:

`Ebar=<m/r^3> diag((c+c^2)/2,(c+c^2)/2,-c-c^2)`.

For positive mass and radial weights, changing shell changes only positive amplitude. At fixed nonzero cap all shells share projective pattern. Hemisphere (`c=0`) is zero for every shell. Caps below and above hemisphere lie on opposite signed projective rays, though magnitudes at `c` and `-c` are generally unequal. Thus zero and signed-ray reversal are cap-boundary effects, not radial-curvature landmarks.

If cap expands as `Theta(ell)=omega ell`, hemisphere crossing is `ell=pi/(2omega)` and free `omega` moves it arbitrarily. Correlated radial-angular measures can break factorization, but resulting shape is measure/protocol information until physical covariant region law is independently derived.

This control uses exact Schwarzschild amplitude and pattern but assumes Euclidean common-space alignment. It is not Schwarzschild bitensor parallel transport, causal four-dimensional region averaging, physical orientation measure, data, or UMCH law. `ell0` is absent. Covariant correlated spacetime windows remain open; no structural dead end or reformulation is triggered.
