# Finite-loop holonomy path-shape landmark

Classification: `TOY_CONTROL_AND_NEGATIVE_RESULT`.

Status: `HOLONOMY_TRACE_LANDMARK_PATH_SHAPE_MOVABLE_NOT_ELL0`; `NO_POSITIVE_DETECTION_CLAIM`.

For shear segment maps `A(a)=[[1,a],[0,1]]` and `B(b)=[[1,0],[b,1]]`, product has determinant one and trace `2+ab`. Protocol choice `a=ell`, `b=-rho ell` gives conjugacy-invariant trace `2-rho ell^2`. For target `tau<2`, crossing is

`ell_cross=sqrt((2-tau)/rho)`.

Free positive path-shape parameter places crossing at any positive target by `rho=(2-tau)/ell_cross^2`. Setting `rho=0` removes crossing; other signs alter existence. Thus survival under similarity/conjugacy quotient is necessary but insufficient: invariant remains path-family nuisance dependent.

If loop family and shape are independently derived from connection, boundary and region protocol, trace crossing can characterize geometry/path scale. It does not identify `ell0`, absent here, without fixed relation `ell_cross=alpha ell0`.

This is 2x2 shear algebra, not spacetime holonomy or observation. Connection-derived fixed loop families remain open; no core reformulation is triggered.
