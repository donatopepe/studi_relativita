# Turnover shape derivation gate

Status: `BLOCKED_PENDING_INDEPENDENT_NONLOCAL_MECHANISM`; `NO_POSITIVE_DETECTION_CLAIM`.

Curvature dimension `L^-2` plus fixed regular small-region area normalization yields `p=2` for tidal, magnetic, and holonomy channels. For clock, null, and congruence channels, `p=2` requires a declared fixed short-baseline geometry and is not universal.

Local Taylor, Jacobi, and holonomy expansions produce powers/higher orders. They do not derive candidate exponential coefficient q. A nonlocal kernel, screening/correlation law, transfer function, or modified dynamics must be independently specified and tested. Introducing q to force turnover is circular.

Raw response vector, C2/Cinfinity, frame, regions, thresholds, and `F_0` remain preregistered. No data, ell0, floor, or detection follows.
