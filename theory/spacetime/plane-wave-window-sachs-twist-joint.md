# Exact plane-wave finite-window/non-vertex Sachs joint object

## Raw cross-channel object

Retain raw dimensionless tuple

`(LW,X,LV,LS,LS_0)`, with `S=VX^{-1}`,

plus centered support, top-hat or triangular kernel, parallel screen, and declared rotating boundary. Twist and matrix norms are dependent diagnostics, not independent channels.

## Exact affine/profile/boundary orbit

For `K_s(u)=s^{-2}K(u/s)`, `L_s=sL`, and dimensionally consistent `S_{0,s}=S_0/s`, scale-covariant kernels give

`L_sW_s=LW`, `X_s=X`, `L_sV_s=LV`, `L_sS_s=LS`, `L_sS_{0,s}=LS_0`.

Thus complete declared raw joint object collides for top-hat and triangular kernels. Any support-coordinate landmark based only on it moves `L_* -> sL_*`.

At fixed curvature profile, support, kernel, and screen, varying boundary twist leaves `LW` unchanged but changes `X,LV,LS,LS_0` and endpoint twist. Joining channels therefore does not eliminate congruence-preparation nuisance. Different profile shapes remain conditionally distinguishable at fixed boundary.

## Ledger

Classification: `EXACT_SPACETIME_CROSS_CHANNEL_SACHS_CONTROL_AND_NEGATIVE_IDENTIFIABILITY_RESULT`.

Status: `EXACT_PLANE_WAVE_WINDOW_SACHS_TWIST_JOINT_PROFILE_AND_BOUNDARY_CONDITIONAL_AFFINE_ORBIT_NOT_ELL0`.

Open gate: `PHYSICAL_CAUSAL_WINDOW_ROTATING_BOUNDARY_COMMON_SCREEN_AND_ELL0_LAW_NOT_DERIVED`.

Coley–McNutt–Milson supports exact vacuum Brinkmann plane waves and curvature-driven geodesic deviation. Chosen finite window/kernel, rotating boundary, common detector screen, affine/profile/boundary nuisance, UMCH, `ell0`, and detection are not source-established.

Detector-derived boundary standards, causal non-scale-covariant windows, independent dimensional calibration, physical screen transport, caustic continuation, and other exact geometries remain open. This is not a structural dead end.

UMCH remains `UNPROVEN`; `NO_POSITIVE_DETECTION_CLAIM`.
