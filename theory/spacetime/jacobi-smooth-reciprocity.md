# Continuous symmetric-profile Jacobi reversal reciprocity

Classification: `PROJECT_DERIVATION_AND_NEGATIVE_RESULT`.

Status: `JACOBI_CONTINUOUS_SYMMETRIC_PROFILE_REVERSAL_BLOCK_TRANSPOSE_RECIPROCITY_NOT_ELL0`; `NO_POSITIVE_DETECTION_CLAIM`.

On fixed affine interval `[0,S]`, consider

`Y'(s)=H(s)Y(s)`, `H(s)=[[0,I],[-K(s),0]]`, `Y(0)=I`,

with real continuous symmetric `K(s)`. Define reversed profile `K_rev(s)=K(S-s)`, block exchange `E=[[0,I],[I,0]]`, and anti-involution `R(P)=E P^T E`.

For each piecewise-constant approximation, every segment exponential is `R`-fixed because its optical matrix is symmetric. Reversing profile reverses product order, hence `P_rev,N=R(P_N)`. Uniform approximation of continuous `K`, convergence of fundamental matrices/ordered products, and continuity of `R` give

`P_rev=R(P)`.

If `P=[[A,B],[C,D]]`, then

`P_rev=[[D^T,B^T],[C^T,A^T]]`,

so `B_rev=B^T`. This promotes previous finite-product pattern to continuous symmetric optical profiles under declared affine, boundary and reversal conventions. Deterministic Runge--Kutta controls verify a rotating smooth anisotropic profile; a nonsymmetric optical matrix breaks identity, confirming symmetry is essential.

Vertex singular values, determinant and Frobenius norm remain reversal-blind. Independent endpoint-frame quotient also identifies `B` and `B^T`; oriented raw entries require a physical screen anchor and transport certificate.

Scope is continuous matrix optical profiles in a fixed trivialized screen bundle. This is not yet a connection-derived screen system in an exact four-dimensional spacetime, detector observable, data result, or UMCH mechanism. No `ell0` appears. Exact-spacetime transport and physical cross-channel laws remain open, so no structural dead end or reformulation follows.
