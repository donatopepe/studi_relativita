# Multiscale operational spacetime-curvature hypothesis

Status: `UNPROVEN`.

Realized universe is globally non-Minkowski and may have preregistered nonzero operational spacetime response above a tested threshold family.

Survey regions/scales form `D_survey`, fixed independently of unknown `ell0` and observed responses. For a candidate model supported only at `x=ell/ell0>=1` (`ℓ≥ℓ₀`), require `ell0<=ell_min` across fixed survey data. Otherwise candidate is `DOMAIN_INCONSISTENT`; no sample may be removed post-fit to restore domain.

A freely falling body may obey `a^μ=0` and `κ_wl=0`. Curved spacetime is measured by relative finite-region effects, so no reaction force is required. Minkowski remains mathematical null model. Domain correction prevents circular selection but neither identifies `ell0` nor establishes positive floor.
