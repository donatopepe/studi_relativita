# Schwarzschild scattering endpoint-clock phase cross-channel control

Status: `SCHWARZSCHILD_STATIC_ENDPOINT_CLOCK_PHASE_ADDS_CROSS_CHANNEL_SHAPE_BUT_RETAINS_EXTERNAL_FREQUENCY_AND_GEOMETRIC_SCALE_BLINDNESS_NOT_ELL0`.

Gate: `PHYSICAL_CLOCK_REALIZATION_SOURCE_COHERENCE_EMISSION_ABSORPTION_SCREEN_PREPARATION_VECTOR_READOUT_JOINT_COVARIANCE_AND_ELL0_LAW_NOT_DERIVED`.

Authority: `UMCH=UNPROVEN`; `ell0_identified=false`; `structural_dead_end=NOT_DECLARED`; `NO_POSITIVE_DETECTION_CLAIM`; `CONFIRMATORY_ANALYSIS_ELIGIBLE_NOT_EVIDENCE`; review `DIRECT_REVIEW_NO_SUBAGENT`.

## Objects kept distinct

For finite equatorial future-null Schwarzschild scattering, `R>rho>3`, equal-radius static endpoints and screen order `(polar,in-plane)`, use `x=r/M`, `beta=b/M`, and turning chart `x=rho+y^2`. Null Killing time is first computed as a project-derived regularized path integral. Static endpoint proper elapsed time is

```text
Delta_tau_R=sqrt(1-2/R) Delta_t.
```

Given declared local source-clock frequency, `nu_s=M omega_s`, define

```text
Phi_clock=nu_s Delta_tau_R/M.
```

Primary joined record is `R_joint=(Phi_clock,P_frequency_converted)`, where `P_frequency_converted` is the complete corrected transported-screen `4x4` Jacobi phase map. `Phi_clock` is a scalar clock-channel entry; it neither scalarizes nor replaces `P`.

Five notions must not be conflated:

1. Schwarzschild coordinate elapsed time `Delta t`;
2. proper elapsed time of a declared static endpoint clock `Delta_tau_R`;
3. phase accumulated by that declared monochromatic toy standard `Phi_clock`;
4. phase/coherence distribution generated by a physical source and selected by emission/absorption dynamics;
5. vector detector readout with calibration and covariance.

Only items 1–3 are constructed. No photon internal clock is asserted. No matched flat reference is provided, so no excess Shapiro-delay observable is claimed.

## Dilation counterexample

At fixed `rho`, `R`, and `nu_s`, Schwarzschild dilation `M -> sM` gives `omega_s -> omega_s/s`, while `Delta_tau_R/M`, `Phi_clock`, and the rate-converted full map are invariant. Deterministically:

```text
Delta_t/M=40.223422979930845
Delta_tau_R/M=36.718793510299655
Phi_clock=7.343803420273261
converted_phase_map_residual=5.9117155615240335e-12
```

Joint rank remains

```text
rank_shape_boundary=2
rank_with_log_M=2
log_M_column_norm=5.730937517383603e-09
scale_null_direction=[0,0,1]
```

Holding dimensional `omega_s` fixed breaks this comparison because the external standard supplies a dimensionful direction; classification is `EXTERNAL_FREQUENCY_STANDARD_DIRECTION_NOT_INTERIOR_GEOMETRIC_SCALE`. It does not derive an interior geometric scale or `ell0`.

Orientation-even clock phase also does not erase raw optical orientation. The zero-window limit sends clock phase toward zero and full map toward identity; it is not holonomy or evidence. Local rank does not establish global injectivity, channel independence or a likelihood without joint covariance.

## Source boundary

`Schwarzschild2003Translation`, `Darwin1959GravityField`, and `Sachs1961` provide bounded canonical context for Schwarzschild geometry/static clocks, null scattering, and optical/Jacobi propagation. They do not establish this full endpoint-clock protocol, physical source coherence, emission action, absorber response, screen preparation, vector detector readout, joint covariance, `ell0`, UMCH evidence or detection.

Result is a `PROJECT_DERIVATION` and `TOY_EXTERNAL_CLOCK` negative control. Physical source/absorber/detector dynamics and other exact geometries remain open routes; no structural dead end follows.
