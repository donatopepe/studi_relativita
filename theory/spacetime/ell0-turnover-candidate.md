# ell0 turnover candidate

Status: `MATHEMATICAL_IDENTIFIABILITY_CANDIDATE_ONLY`; `NO_POSITIVE_DETECTION_CLAIM`.

Candidate, not adopted UMCH law: `F_T=A x^p exp[-q(x-1)]`, `x=ell/ell0>=1`, with `A>0` and independently theory-fixed `p>q>0`.

Two positive responses give

`ell0=q(ell1-ell2)/[p log(ell1/ell2)-log(y1/y2)]`.

Amplitude cancels. Log-elasticity vanishes at `ell_peak=(p/q)ell0`. These facts establish structural identifiability only under fixed shape.

If p or q is fitted jointly, if frame is unresolved, or if scale coverage cannot resolve shape/turnover, inference is blocked. Raw vector, channel, C2/Cinfinity, regions, uncertainties, `F_0`, and competing families remain preregistered. No post-data adoption of `F_T`.

No geometric derivation currently fixes p,q. No real data, physical floor, or ell0 value follows.
