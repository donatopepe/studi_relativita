# Orientation-family isotropization

Classification: `TOY_CONTROL_AND_NEGATIVE_RESULT`.

Status: `ORIENTATION_WEIGHT_LANDMARK_PROTOCOL_MOVABLE_NOT_ELL0`; `NO_POSITIVE_DETECTION_CLAIM`.

For anisotropic `A=diag(3,1)` and rotated `B=diag(1,3)`, orientation mixture `R(w)=wA+(1-w)B` changes projective spectrum and becomes isotropic at `w=1/2`. Eigenvalue multiplicity change is therefore generated solely by orientation weights.

If finite-window scale changes sampled orientation measure, any chosen target scale can host isotropy by selecting nuisance profile satisfying `w(ell_target)=1/2`. Full symmetric orientation averaging erases anisotropy. Such crossing/multiplicity landmarks are protocol-movable unless orientation family and measure are independently fixed.

Conjugacy quotient alone is insufficient: each rotated operator is conjugate to `A`, but their convex mixture generally has different spectrum and is not conjugate to `A`. One must fix whether protocol transports then averages, averages raw coordinates, or averages conjugacy invariants.

`ell0` is absent. A physical result needs covariant region/orientation measure, frame/transport certificate, boundary data and derived scale link.

This is a 2x2 mixture toy, not spacetime derivation or observation. Exact-region routes remain open; no core reformulation is triggered.
