# Schwarzschild mixed-plane Levi-Civita holonomy

Classification: `EXACT_SPACETIME_LEVI_CIVITA_HOLONOMY_CONTROL_AND_NEGATIVE_IDENTIFIABILITY_RESULT`.

Status: `EXACT_SCHWARZSCHILD_MIXED_PLANE_LEVI_CIVITA_HOLONOMY_NONABELIAN_PATH_ORDERED_BOUNDARY_DEPENDENT_AND_GEOMETRIC_SCALE_BLIND_NOT_ELL0`.

Scope: `FOUR_DIMENSIONAL_SCHWARZSCHILD_LEVI_CIVITA_CONNECTION_ON_MATHEMATICAL_PIECEWISE_COORDINATE_LOOPS_NOT_DETECTOR_DERIVED`.

Gate: `PHYSICAL_CAUSAL_LOOP_FAMILY_PROPER_TIME_LENGTH_STANDARD_TETRAD_ANCHOR_DETECTOR_READOUT_AND_ELL0_LAW_NOT_DERIVED`.

For exterior Schwarzschild `f=1-2M/r`, integrate `dV/ds=-Gamma_mu dz^mu/ds V` around equatorial `t-r` and `r-phi` rectangles. Read both at a common static oriented orthonormal tetrad. Their raw matrices `H_tr` and `H_rphi` are nonidentity, and their ordered products differ. Orientation reversal obeys `H_reverse=H^-1` numerically.

A shrinking rectangle approaches the local curvature-flux exponential, but for a finite rectangle `H is not assumed equal to exp(integral R)`. The measured mismatch records radial connection variation, boundary and path ordering. It is a dependent representation of the same metric/connection history: `PATH_ORDERED_CONNECTION_HISTORY_REQUIRED`, not an independent observational channel.

Equal coordinate-area rectangles at different radial boundaries yield different raw holonomies. Thus finite response is nonradial in window placement and boundary shape. Conversely, characteristic coefficients collide under reversal and common tetrad changes act by conjugation, so anchored raw distinctions do not survive every nuisance quotient.

The orbit `(M,r,T)->(sM,sr,sT)` at fixed angular aperture preserves dimensionless tetrad holonomies while proper scales change. Therefore the control is blind to absolute geometric scale and contains no `ell/ell0` law. Flat and shrinking-loop controls tend to identity.

These are mathematical piecewise-coordinate loops: not geodesic, not causal, not detector-derived. Non-Abelianity does not imply independent rank. `ell0_identified=false`; UMCH is `UNPROVEN`; result is `NO_POSITIVE_DETECTION_CLAIM`; `structural_dead_end=NOT_DECLARED`.
