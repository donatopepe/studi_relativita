# Schwarzschild photon-sphere finite arc in radial-geodesic endpoint frames

Classification: `PROJECT_DERIVATION_AND_TOY_ENDPOINT_CONTROL_WITH_NEGATIVE_INTERIOR_SCALE_IDENTIFIABILITY_RESULT`.

Status: `SCHWARZSCHILD_PHOTON_SPHERE_FINITE_ARC_FREEFALL_ENDPOINT_FRAMES_EXPOSE_PREPARATION_DIRECTIONS_BUT_RETAIN_ONE_INTERIOR_SHAPE_DIRECTION_AND_GEOMETRIC_SCALE_BLINDNESS_NOT_ELL0`.

Scope: `FOUR_DIMENSIONAL_SCHWARZSCHILD_FUTURE_NULL_PHOTON_SPHERE_FINITE_ARC_WITH_LOCAL_RADIAL_GEODESIC_ENDPOINT_TETRADS_PROJECT_PHASE_RATE_CONVERSION_TOY_PREPARATION_LABELS_AND_NO_DETECTOR_READOUT`.

Gate: `PHYSICAL_RELEASE_HISTORY_FINITE_ARC_WINDOW_SOURCE_OBSERVER_SYNCHRONIZATION_SCREEN_PREPARATION_FREQUENCY_STANDARD_CAUSTIC_CONTINUATION_VECTOR_READOUT_COVARIANCE_AND_ELL0_LAW_NOT_DERIVED`.

UMCH remains `UNPROVEN`; `NO_POSITIVE_DETECTION_CLAIM`; `ell0_identified=false`; `structural_dead_end=NOT_DECLARED`.

## Construction

At `r=3M`, `f=1/3`. A local radial timelike geodesic state satisfies

```text
u^t=E/f,  u^r=sigma sqrt(E^2-f),  g(u,u)=-1.
gamma=E/sqrt(f),  beta=sigma sqrt(1-f/E^2).
```

The coordinate normalization, geodesic acceleration and endpoint-tetrad Lorentz residuals are directly tested. `E=1` is a marginally-bound toy label, not a derived release history. The static limit is `E=sqrt(f)`.

Raw interior objects remain the open-arc connection map `T_arc` and full optical phase map `P_arc` from the prior exact control. Endpoint maps are

```text
T_ff = B_o^{-1} T_arc B_s,
omega(E)=sqrt(3) E,
D(omega)=diag(I,I/omega),
P_ff = D(omega_o)^{-1} P_arc D(omega_s).
```

These are explicit endpoint basis/rate actions, not detector response. Reconstructing with declared endpoint actions returns static interior maps. Unequal frames at zero window produce `ENDPOINT_FRAME_COMPARISON_NOT_HOLONOMY_OR_INTERIOR_CURVATURE_RESPONSE`. Open transport is not holonomy.

## Counterexamples

Composition succeeds only with matched intermediate frame and phase-rate conventions. Deliberate mismatch gives large residuals; inserting explicit transition restores composition. Hence endpoint labels cannot silently be treated as shared calibration.

At `alpha=pi` and `alpha=2*pi`, transformed vertex `B` blocks remain singular: `CAUSTIC_OR_CONGRUENCE_BLOCK_SINGULAR`. Full phase maps remain finite and invertible. Graph objects do not replace full maps at caustics.

Two endpoint preparations produce different raw `T_ff` and `P_ff`, but identical reconstructed static interior objects. Thus raw multiplicity is preparation dependence unless physical endpoint preparation/readout is derived.

Affine normalization and Schwarzschild geometric dilation remain separate exact nuisances. After declared conversions, both residuals are numerical zero within tolerance.

For fixed endpoint preparation, the Jacobian versus `(alpha,log M)` has `rank_fixed_preparation=1`, `scale_null_direction=[0,1]`, and `independent_channels=false`. Allowing `E` to vary gives a nonzero derivative, but it is `ENDPOINT_PREPARATION_DIRECTION_NOT_INTERIOR_GEOMETRIC_SCALE`; it is not evidence for `ell0` or an independent physical channel.

The elliptic optical subblock has a `2*pi` collision while the joint raw map does not. Local rank still does not prove global injectivity. At `alpha=2*pi`, the static future-null segment matches prior photon-orbit provenance; no freefall closure is derived.

## Source authority and limits

`Schwarzschild2003Translation` supplies Schwarzschild-solution context; `Darwin1959GravityField` supplies Schwarzschild trajectory and critical circular-orbit context; `Sachs1961` supplies null optical framework. Radial endpoint state, boosts, phase-rate conversions, quotients and rank diagnostics are project derivations/toy controls. Sources do not establish release history, endpoint synchronization, screen preparation, frequency standard, detector, vector readout, covariance, `ell0`, UMCH, evidence or detection.

Passing means only `CONFIRMATORY_ANALYSIS_ELIGIBLE_NOT_EVIDENCE`. Generic nonradial scattering and detector-derived calibration/readout remain open, so no structural dead end is declared.
