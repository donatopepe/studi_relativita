# Schwarzschild photon-sphere finite-arc connection/Jacobi cross-map

Classification: `EXACT_SPACETIME_CROSS_CHANNEL_CONTROL_AND_NEGATIVE_IDENTIFIABILITY_RESULT`.

Status: `SCHWARZSCHILD_PHOTON_SPHERE_FINITE_ARC_CONNECTION_JACOBI_CROSS_MAP_CAUSTIC_LANDMARKED_LOCALLY_ONE_SHAPE_DIRECTION_AFFINE_AND_GEOMETRIC_SCALE_BLIND_NOT_ELL0`.

Scope: `FOUR_DIMENSIONAL_SCHWARZSCHILD_FUTURE_NULL_PHOTON_SPHERE_FINITE_ARC_CONNECTION_AND_SCREEN_PHASE_MAP_WITH_PROJECT_AFFINE_NORMALIZATION_TOY_ENDPOINT_BASES_AND_NO_DETECTOR_READOUT`.

Gate: `PHYSICAL_FINITE_ARC_WINDOW_SELECTION_SOURCE_OBSERVER_TETRADS_SCREEN_PREPARATION_AFFINE_FREQUENCY_STANDARD_CAUSTIC_CONTINUATION_VECTOR_READOUT_COVARIANCE_AND_ELL0_LAW_NOT_DERIVED`.

UMCH remains `UNPROVEN`; detection remains `NO_POSITIVE_DETECTION_CLAIM`; `ell0_identified=false`; `structural_dead_end=NOT_DECLARED`. Passing deterministic gates gives at most `CONFIRMATORY_ANALYSIS_ELIGIBLE_NOT_EVIDENCE`.

## Open arc and raw objects

At `r_ph=3M`, use the project anchor `k=e_0+epsilon e_3`. For arc angle `alpha`,

`L=3M alpha`, `Delta phi=epsilon alpha`, `Delta t=3 sqrt(3) M alpha`.

`alpha` is a dimensionless window/protocol parameter, not `ell/ell0`. Except at integer full windings, the path is open. Its Levi-Civita endpoint map is therefore `OPEN_ARC_ENDPOINT_TRANSPORT_NOT_HOLONOMY`, not a holonomy.

Two raw channel-native objects are preserved:

1. `T_arc`, the `4x4` tetrad-represented connection endpoint map;
2. `P_arc=[[A,B],[C,D]]`, the full `4x4` screen phase map generated by `[[0,I],[K,0]]`, with `K=diag(1/(9M^2),-1/(9M^2))` in the audited `X''=KX` convention.

Characteristic coefficients, blocks, spectra surrogates, Jacobians and ranks are derived diagnostics. They never replace `T_arc` or `P_arc`.

## Exact controls and boundaries

At `alpha=0`, both maps are identity and finite-difference derivatives recover their declared generators. Because the photon-sphere generators are constant in the chosen trivializations, both families satisfy exact semigroup composition. Exact matrix exponentials agree with independent fixed-step transport/RK4 propagation; the maps satisfy Lorentz/symplectic constraints.

For vertex data, the oscillatory screen phase is `sqrt(|K|)L=alpha`. Conjugate points occur at `alpha=pi` and `alpha=2*pi` in the audited range. There `B` is singular and `S=DB^{-1}` returns `CAUSTIC_OR_CONGRUENCE_BLOCK_SINGULAR`; the full phase map stays finite and invertible. A separately declared nonvertex boundary remains regular. Caustics are graph-domain gates, not UMCH signals.

Orientation reversal changes raw connection transport but its characteristic coefficients collide in this symmetric family. The diagonal optical map is orientation blind. Common endpoint conjugacies alter raw entries while preserving the declared characteristic quotient. These collisions are negative controls: tetrad handedness, endpoint labels and screen calibration were not derived from a detector.

## Scale and cross-channel rank

Affine rescaling is tested with `D_A_EQUALS_DIAG_I_I_OVER_A`. Schwarzschild geometric scaling is tested separately:

`(M,r_ph,L,Delta_t)->s(M,r_ph,L,Delta_t)_WITH_PHASE_RATE_CONVERSION`.

At fixed `alpha`, tetrad `T_arc` and rate-converted `P_arc` collide while dimensional affine length and coordinate duration change. The photon-sphere radius and caustic angles are geometric/protocol landmarks, not an `ell0` landmark.

A preregistered derived feature concatenates characteristic coefficients of the connection map and the rate-converted phase map. Its finite-difference Jacobian with parameters `(alpha,log_M)` has `rank_joint=1`: the `alpha` direction is nonzero and the scale direction is null. Consequently `independent_channels=false` for parameter counting in this exact shared-geometry family. Two operator maps and many varying entries do not create two physically calibrated continuous shape directions.

This is a bounded local rank result, not a universal theorem. The elliptic phase subblock is periodic under `alpha->alpha+2*pi`, but the hyperbolic/full joint map does not collide in the tested pair. Absence of that global collision does not identify absolute scale.

## Closed-loop provenance and source scope

At `alpha=2*pi`, `T_arc` reproduces the future-null segment of the prior photon-orbit control. Adding the same past-directed static closure reproduces its closed-loop map; closure is `DERIVED_PAST_DIRECTED_STATIC_CLOSURE_CROSS_CHECK_ONLY`. Open transport, closure and Jacobi propagation share path and geometry and are not counted as independent observations.

`Darwin1959GravityField` supports Schwarzschild trajectory and critical circular-orbit context only. `Sachs1961` supports the null optical framework only. Neither source establishes the finite-arc protocol, project affine anchor, endpoint tetrads, screen preparation, window selection, caustic continuation/readout, detector, covariance, `ell0`, UMCH, evidence or detection.

Generic nonradial scattering, freely falling endpoints, and physical detector/readout/covariance derivation remain bounded open routes. No structural dead end or reformulation follows.
