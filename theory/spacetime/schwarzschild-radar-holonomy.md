# Schwarzschild static-radar Levi-Civita holonomy

## Status and scope

`SCHWARZSCHILD_STATIC_RADAR_CAUSAL_BOUNDARY_HOLONOMY_PROTOCOL_DEPENDENT_AND_GEOMETRIC_SCALE_BLIND_NOT_ELL0`

`FOUR_DIMENSIONAL_SCHWARZSCHILD_LEVI_CIVITA_CONNECTION_ON_IDEAL_STATIC_OBSERVER_RADAR_BOUNDARY_WITH_UNDERIVED_MIRROR_AND_READOUT`

Gate: `PHYSICAL_FREELY_FALLING_ENDPOINTS_MIRROR_ACTION_VECTOR_READOUT_COMMON_STANDARD_AND_ELL0_LAW_NOT_DERIVED`.

UMCH is `UNPROVEN`; inference state is `NO_POSITIVE_DETECTION_CLAIM`; structural dead end is `NOT_DECLARED`.

## Project derivation

For `f(r)=1-2M/r` and `r_*(r)=r+2M log(r/(2M)-1)`, radial null curves satisfy `dt/dr=+-f^-1`. A static observer at `r_o=7M` and ideal static mirror at `r_m=4M` define two future null legs and a past observer-worldline closure. The measured round-trip time is

`Delta tau=2 sqrt(f(r_o)) [r_*(r_o)-r_*(r_m)]`.

Parallel transport around this mathematical closed boundary gives a base-static-tetrad matrix `H_radar`. Reflection selects the return path; no vector-reflection action is inserted. Raw record retains geometry, tetrads, vertices, labels, null residuals, segment maps, matrix, orientation, characteristic coefficients, curvature/window summary, refinement, endpoint family, scaling and null controls.

## Exact bounded findings

- `Delta tau=8.168553570818094`; maximum null residual is `3.552713678800501e-15`.
- `||H_radar-I||=0.22713493607514745`; Lorentz residual is `7.338356754219182e-12`.
- Matched radial endpoints and coordinate duration do not make the causal boundary equal to the earlier rectangle: raw difference `0.8239284342654838`.
- Fixed `Delta tau/M` permits different observer/mirror radii, so `duration_only_identifies_boundary=false`. Raw matrices separate the tested anchored toy pair, but identification of tetrads at distinct events is a declared family convention, not detector-derived calibration.
- Reversal changes raw matrix while characteristic coefficients collide. A common spatial tetrad rotation acts by conjugation and preserves characteristic coefficients. Orientation and anchor information therefore cannot be inferred from spectrum alone.
- Under `(M,r_o,r_m)->s(M,r_o,r_m)`, `Delta tau/M` and tetrad holonomy remain fixed; at `s=1.47`, holonomy residual is `1.798766884999431e-16` while proper time changes.
- Flat and shrinking controls approach identity.

Causal support produces nonradial finite-window dependence on boundary and connection history, but no dependence on `ell/ell0`. `ell0_identified=false`; `positive_detection_claim=false`; `structural_dead_end=NOT_DECLARED`.

Cross-channel gate: `TRAVEL_TIME_CURVATURE_AND_HOLONOMY_SHARE_DECLARED_GEOMETRY_AND_ARE_NOT_ASSUMED_INDEPENDENT`. Travel time, curvature and holonomy are dependent outputs of one declared geometry/protocol until a detector and joint covariance model establish otherwise. non-Abelianity does not imply independent rank.

## Source boundary

Lin 2020, DOI `10.1103/PhysRevD.101.124001`, supports observational/radar-coordinate comparisons for localized observers including Schwarzschild-like cases. It does not derive this ideal mirror, finite Levi-Civita loop, closure, vector readout, endpoint collision, `ell0`, UMCH or detection. Schwarzschild and Ambrose--Singer sources retain their previously audited narrow geometric scope.
