# Sphere finite-window path-scale landmark

Classification: `KNOWN_RESULT_PLUS_PROJECT_DERIVATION_AND_NEGATIVE_RESULT`.

Status: `SPHERE_WINDOW_NONRADIAL_LANDMARK_CURVATURE_RADIUS_PATH_SHAPE_NOT_ELL0`; `NO_POSITIVE_DETECTION_CLAIM`.

On radius-`r` sphere, `K=1/r^2`. Prescribe loop/window family whose enclosed path-difference area is `A(ell)=eta ell^2`, where dimensionless `eta>0` is path-shape coefficient. Gauss--Bonnet gives `alpha(ell)=eta ell^2/r^2`.

For equal two-path mixture of anisotropic operator, spectral gap is

`gap(ell)=Delta |cos(eta ell^2/r^2)|`.

This is exact connection-derived nonradial scale shape. First isotropy occurs at

`ell_iso=r sqrt(pi/(2 eta))`.

Free `eta` moves reported `ell` landmark to any positive target. In physical enclosed-area coordinate, first isotropy is fixed at `A_iso=pi r^2/2`. Only ratio `eta/r^2` enters phase, so unknown curvature radius and path shape are confounded. Absolute cosine creates orientation loss and repeated branches.

With independently fixed `eta` and known first branch, response identifies geometric `ell/r`, not universal `ell0`, absent here. Gauss--Bonnet source scope is recorded as `Peskin2026Gauss`; it does not establish UMCH.

Exact two-sphere with prescribed area law only, not four-dimensional spacetime window or physical path selection. No core reformulation is triggered.
