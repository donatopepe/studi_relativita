# Exact plane-wave full-map canonical-calibration reduction

## Objective

Attack remaining statement that endpoint shear leaves `B` and therefore does not erase every full-map invariant. Keep exact Brinkmann plane-wave Jacobi propagators and labelled source/observer endpoints. Enlarge only endpoint calibration nuisance from lower canonical shears to independently free linear canonical phase-space bases:

\[
P\mapsto G_o P G_s^{-1},\qquad G_s,G_o\in Sp(4,\mathbb R).
\]

Determine exact orbit structure before interpreting any full-map spectral or block landmark.

Classification: `EXACT_SPACETIME_CROSS_CHANNEL_CONTROL_AND_NEGATIVE_IDENTIFIABILITY_RESULT`.

## Candidate routes

1. **Shears only.** Already tested; leaves `B` unchanged and is not transitive.
2. **Common canonical basis `G_o=G_s`.** Conjugation preserves similarity invariants and is not transitive.
3. **Independent endpoint canonical bases.** Selected as strongest bounded nuisance test. It preserves endpoint labels but permits independent source/observer phase-space calibration.

## Exact algebra

Every Jacobi propagator generated by a self-adjoint optical tidal matrix is symplectic. For any two such propagators `P_1,P_2`, choose

\[
G_s=I,\qquad G_o=P_2P_1^{-1}.
\]

Since products and inverses of symplectic matrices are symplectic,

\[
G_oP_1G_s^{-1}=P_2.
\]

Thus the independent `Sp(4)xSp(4)` endpoint action is transitive on the symplectic propagator family. All functions of `P` alone become constant after this quotient. This is an exact group-action result, not a numerical fit.

A common calibration is different: `P -> GPG^{-1}` preserves trace and characteristic polynomial. The control must exhibit two exact plane-wave profile propagators with different traces, proving they cannot collide under common conjugation even though they collide under independent endpoint calibration.

## Scope and stop condition

Free `Sp(4)xSp(4)` is a deliberately strongest project nuisance group. Canonical source does not establish that a physical detector permits arbitrary mixing of displacement and derivative, independent endpoint calibration, or unknown gains. Therefore transitivity is conditional on this nuisance contract and is not a universal observational no-go.

Affine/profile rescaling remains an exact subgroup-like collision after dimensionless normalization; no `ell0` occurs. Physical restrictions on endpoint calibration, common standards, tetrads, transport, measured variables, causality, leakage, and full Sachs observables remain open. This is not a structural dead end.

Expected status: `EXACT_PLANE_WAVE_FULL_MAP_TRANSITIVE_UNDER_INDEPENDENT_CANONICAL_ENDPOINT_CALIBRATION_NOT_ELL0`.

Open gate: `PHYSICAL_CANONICAL_ENDPOINT_CALIBRATION_GROUP_NOT_DERIVED`.

UMCH remains `UNPROVEN`; conclusion remains `NO_POSITIVE_DETECTION_CLAIM`.
