# Paper II-A — Hard-Constraint Dynamics and Conditional No-Go Gate

## Status

Approved next milestone after fixed Candidate B rejection. Core `κ₀>0` remains `UNPROVEN`.

## Fixed candidate

Analyze pointwise feasible set

\[
\kappa\ge\kappa_0>0
\]

through normalized inequality

\[
g=1-\kappa/\kappa_0\le0,
\]

and multiplier worldline action

\[
S_A=S_0+mc\int ds\,\lambda(s)g(s),
\qquad \lambda\ge0,\quad \lambda g=0.
\]

`λ` is dimensionless in this fixed convention. No later normalization/sign change may obscure results; alternative formulations become new candidates.

## Objective

Determine whether this hard pointwise implementation can provide consistent dynamics sufficient to open Paper III, or whether it is `CONTRADICTED_UNDER_ASSUMPTIONS`. If both fixed A and B fail, formulate only a conditional no-go for these two analyzed pointwise mechanisms, not for all UMCH.

## Required analysis

1. Covariant variation on inactive and active branches.
2. Distributional/nonsmooth issues at active-set transitions.
3. Feasible initial-data topology and geodesic exclusion.
4. Constraint/multiplier role without inventing Dirac class.
5. Constant-active solutions and local perturbations where derivable.
6. `κ₀→0` set, solution, and multiplier-force limits.
7. Existence/uniqueness/well-posedness evidence or explicit gap.
8. Observable mapping.
9. Decision and Paper III gate.

## Method boundary

KKT algebra is finite-dimensional pointwise feasibility evidence only. Worldline dynamics requires variational analysis. Nonsmooth optimization analogies cannot substitute a field/trajectory constraint derivation.

Published curvature-action formulas may be used on smooth active intervals where multiplier makes an effective `L(κ,s)`, but source assumptions must match. Transition points require separate treatment.

## Preregistered gate

Candidate A must satisfy all:

- nonempty physically acceptable feasible data including controlled standard limit;
- covariant equations on all branches;
- defined matching/transition rules;
- well-posed evolution evidence;
- no required violation of precision-tested free fall for all positive parameter space;
- observable mapping.

Failure of any load-bearing gate yields `CONTRADICTED_UNDER_ASSUMPTIONS` for fixed A. Unresolved optional details alone yield `INCOMPLETE`.

## Conditional no-go rule

If A is contradicted while B remains contradicted, report:

> Under the declared timelike pointwise definition and the two fixed implementations—hard KKT constraint and preregistered smooth barrier—neither supplies a viable foundation under required gates.

This does not exclude every action, nonlocal formulation, field/vacuum sector, or separately ratified RMS hypothesis.

## Artifacts

- sourced hard-constraint variational analysis;
- deterministic branch/limit checks;
- bilingual audit and LaTeX appendix;
- updated decision matrix and roadmap;
- tests and CI;
- explicit Paper III gate.

## Out of scope

Changing Candidate B, Candidate C, ALD, QED, gravity, cosmology, quantization, curved spacetime, external force fitting, or declaring global UMCH falsification.
