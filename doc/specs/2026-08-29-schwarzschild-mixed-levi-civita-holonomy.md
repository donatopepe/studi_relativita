# Schwarzschild mixed-plane Levi-Civita holonomy control

## Status and bounded question

Classification: `KNOWN_RESULT`, `PROJECT_DERIVATION`, `TOY_CONTROL`, and `NEGATIVE_RESULT`, kept separate.

Question: after the Einstein-static control showed genuine non-Abelian spatial holonomy but exact elementary window dependence, do finite loops in an exact inhomogeneous four-dimensional spacetime produce nonradial raw transport shape or an identifiable absolute scale?

UMCH remains `UNPROVEN`; no data, mechanism, `ell0`, or detection is introduced.

## Alternatives considered

1. **Schwarzschild mixed coordinate rectangles — selected.** Equatorial `t-r` and `r-phi` loops at a common base point probe boost-like and rotation-like Levi-Civita transport in an inhomogeneous exact vacuum geometry. Connection matrices vary radially, so finite holonomy retains boundary/path ordering beyond a single constant-curvature exponential.
2. **de Sitter mixed loops.** Exact and Lorentzian, but constant curvature risks repeating the Einstein-static area-only collapse.
3. **Kerr loops.** More generic and physically rich, but frame dragging, horizon domains, branch choices and larger connection algebra obscure the first inhomogeneous control.

## Geometry, domain, frame and loops

Use Schwarzschild coordinates outside the horizon,

`ds^2=-f dt^2+f^-1 dr^2+r^2 dtheta^2+r^2 sin^2(theta)dphi^2`, `f=1-2M/r`, with `M>0`, `r0>2M`, `r1>r0`, and `theta=pi/2`.

At common base event retain the static oriented orthonormal tetrad. Construct positively oriented coordinate rectangles `L_tr(T;r0,r1)` and `L_rphi(Phi;r0,r1)`. Every segment is explicitly labelled. Parallel transport solves `dV/ds=-Gamma_mu(z(s)) dz^mu/ds V`; deterministic step refinement supplies convergence evidence. These are mathematical piecewise-coordinate loops, not claimed geodesic, causal, detector-realizable, or freely falling.

## Raw records and controls

Retain `M`, metric, tetrad, `Gamma_mu(z)`, vertices, orientations, segment transports, full coordinate and tetrad holonomies, ordered products, commutator, spectra/characteristic coefficients, finite-window curvature-flux approximants, refinement history, boundary-shape controls, scaling orbit and flat/null controls.

Acceptance tests:

1. metric-compatible connection and tetrad Lorentz preservation;
2. nonidentity and reversal/inverse relation;
3. noncommuting common-base `t-r` and `r-phi` holonomies;
4. convergence under step doubling;
5. shrinking-loop agreement with the local curvature generator to declared order, without claiming finite `H=exp(integral R)`;
6. finite-loop residual from a naive single curvature-flux exponential, demonstrating path/boundary ordering rather than an independent channel;
7. equal coordinate-area or equal proper-area windows can differ in raw holonomy when radial placement/boundary differs;
8. trace/spectrum and common tetrad conjugation lose anchored raw information;
9. simultaneous geometric scaling `(M,r,T)->(sM,sr,sT)` with fixed `Phi` preserves dimensionless tetrad holonomies;
10. `M->0` or shrinking windows approach identity;
11. explicit `ell0_identified=false`, `positive_detection_claim=false`, `structural_dead_end=NOT_DECLARED`.

## Identifiability interpretation

Finite inhomogeneous windows may generate nonradial raw dependence on compactness, radial ratios, duration ratio and angular aperture. That is geometry/protocol dependence, not an `ell0` landmark. Non-Abelianity, path ordering, multiple loop planes and finite-versus-local curvature-flux mismatch do not imply independent latent rank: all are functions of the same metric, connection and declared boundary.

The exact scaling orbit removes absolute length. A physical proper-time/length standard, causal loop family, endpoint tetrads, detector action/readout, sampling and a geometry law containing `ell/ell0` remain required.

## Source boundary and stop rule

Canonical Schwarzschild/parallel-transport sources may support the metric, Levi-Civita transport and holonomy-curvature context only. They do not support this finite loop family, numerical protocol, detector interpretation, `ell0`, UMCH or detection.

This is not a structural dead-end declaration. Generic exact geometries, causal windows, physically selected boundaries and detector-restricted quotient groups remain open.
