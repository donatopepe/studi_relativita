# Operator-valued multiscale response reformulation

## Ratified correction

UMCH primary object is not a planar scalar curve and not merely a curve in R6. Spacetime curvature is intrinsic, multidirectional, and tensor/operator-valued. The scalar threshold route, including turnover `F_T` and q, becomes a secondary projection-specific branch.

## Primary response object

For region, scale, physical frame, orientation/screen/congruence/circuit, and protocol data P, define a response section

`mathscr R[Omega,ell,u_phys,P;g] in E_(Omega,ell,u,P)`

whose fibers contain channel-native objects:

- tidal and magnetic spatial endomorphisms/tensors;
- holonomy maps/classes over finite loops;
- clock-network residual matrices/cochains;
- null Jacobi maps on screen bundles;
- congruence expansion/shear/vorticity fields.

The six-channel raw record remains an index of channel families, not assertion that each channel is one scalar. `C_2/C_infinity` remain preregistered descriptive scalarizations only.

## Identifiability routes

Before nonlocal q physics, test amplitude-independent invariants of operator family:

- normalized/projective channel direction;
- eigenvalue ratios and spectral flow;
- rank/nullity and multiplicity changes;
- holonomy conjugacy/singular values;
- crossings and anisotropy;
- caustic/parity structure;
- cross-channel maps with joint dependence model.

A scale `ell0` is identifiable only if a preregistered invariant map is injective in `x=ell/ell0`, or has a theory-fixed landmark `ell*=alpha ell0`, after nuisance quotient. No landmark is currently derived.

## Flat comparator and falsification

Compare channel-native object with matched Minkowski object under fixed protocol. `FRAME_UNRESOLVED`, domain failure, dependence failure, or noninjective invariant remains nonconfirmatory.

## Status changes

- scalar threshold/no-go remains valid in exact scalar scope;
- turnover/q branch remains mathematical and secondary;
- no universal q derivation is required unless scalar `F_T` branch is chosen;
- operator reformulation is `RATIFIED_PRIMARY_RESEARCH_OBJECT`, not evidence;
- operator identifiability is `NOT_YET_DERIVED`;
- core remains `UNPROVEN`, no detection.

## Acceptance

Bilingual authoritative paper/spec/theory, deterministic operator toy controls showing a projective-identifiable case and a collinear non-identifiable counterexample, updated ledgers/roadmap, tests/CI. No physical ell0 value or mechanism invented.
