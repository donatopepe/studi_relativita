# Schwarzschild static-radar finite-window holonomy control

## Bounded question

Can the mathematical `t-r` rectangle from the prior Schwarzschild control be replaced by a physically interpretable causal boundary generated by one static observer's emitted and returned radial null rays, without creating a false absolute-scale or independent-channel claim?

Classification remains separated: Schwarzschild metric and null geodesics are `KNOWN_RESULT`; endpoint elimination, transport and collision tests are `PROJECT_DERIVATION`; idealized mirror/radar construction is `TOY_CONTROL`; identifiability failures are `NEGATIVE_RESULT`.

UMCH remains `UNPROVEN`; no data, detector action, mechanism, `ell0`, or detection is introduced.

## Alternatives

1. **Static-observer radial radar loop — selected.** One observer at `r_o` emits a radial null ray to an ideal mirror at `r_m`, receives its reflection, then closes vector comparison along the observer worldline. Null legs are causal and selected by an explicit idealized protocol. Static endpoints give exact tortoise-coordinate travel time.
2. **Freely falling emitter/mirror.** More physical for geodesic instruments, but moving endpoints and synchronization add energy/initial-data nuisance before the static causal control is understood.
3. **Optical Jacobi bundle.** Closer to lensing data, but radial spherical symmetry makes screen response degenerate and requires source shape/covariance assumptions.

## Geometry and protocol

Use exterior Schwarzschild `f(r)=1-2M/r`, `M>0`, `2M<r_m<r_o`. Static observer and ideal static mirror carry outward-oriented static tetrads. The observer emits at proper time `tau=0`; radial null legs satisfy

`dt/dr=+-f^-1`,

so coordinate half-time is

`Delta t=r_*(r_o)-r_*(r_m)`,
`r_*(r)=r+2M log(r/(2M)-1)`,

and round-trip observer proper time is

`Delta tau=2 sqrt(f(r_o)) Delta t`.

The closed spacetime path consists of ingoing future null leg, outgoing future null leg after ideal reflection, and past-directed observer-worldline closure. Parallel transport obeys `dV/ds=-Gamma_mu dz^mu/ds V`. Reflection selects path only; it does not apply an invented vector-reflection law to the transported test vector.

Scope label:

`FOUR_DIMENSIONAL_SCHWARZSCHILD_LEVI_CIVITA_CONNECTION_ON_IDEAL_STATIC_OBSERVER_RADAR_BOUNDARY_WITH_UNDERIVED_MIRROR_AND_READOUT`.

## Raw record

Retain `M,r_o,r_m,f,r_star,Delta_t,Delta_tau`, observer/mirror tetrads, labelled vertices/segments, null residuals, segment transports, coordinate and base-tetrad holonomy `H_radar`, reversal, characteristic coefficients, local curvature/window summaries, refinement history, fixed-duration collision family, scaling orbit, flat and shrinking controls.

## Counterexample-first gates

1. Verify null legs, metric compatibility, Lorentz preservation, reversal and refinement convergence.
2. Compare causal radar holonomy with coordinate rectangle having same radial endpoints and coordinate duration; any equality or difference is classified before interpretation.
3. Hold measured dimensionless round-trip duration `Delta tau/M` fixed while varying mirror compactness. Solve the endpoint relation and test whether holonomy varies: duration alone need not identify boundary or connection history.
4. Test whether joint `(Delta tau/M,H_radar)` distinguishes the bounded one-parameter mirror family in an anchored tetrad, then repeat after characteristic/conjugacy quotient. Raw injectivity in this toy family is not detector-derived identifiability.
5. Test exact scaling `(M,r_o,r_m,Delta t,Delta tau)->s(...)`: dimensionless raw tetrad holonomy and duration ratio must remain unchanged while all proper scales change.
6. Test flat and shrinking-radar limits.
7. Keep holonomy dependent on declared connection/path; do not count travel time, local curvature, transport matrix or its spectrum as independent channels without a physical joint covariance/readout model.

## Interpretation and stop rule

Causal support and observer proper time improve protocol semantics over arbitrary rectangles, but static acceleration, mirror placement, ideal reflection, tetrad orientation and vector-comparison readout remain selected nuisances. A radar time provides an operational length only in declared geometry and units; it does not generate `ell/ell0`.

Expected bounded result: `SCHWARZSCHILD_STATIC_RADAR_CAUSAL_BOUNDARY_HOLONOMY_PROTOCOL_DEPENDENT_AND_GEOMETRIC_SCALE_BLIND_NOT_ELL0` unless tests falsify it.

Gate: `PHYSICAL_FREELY_FALLING_ENDPOINTS_MIRROR_ACTION_VECTOR_READOUT_COMMON_STANDARD_AND_ELL0_LAW_NOT_DERIVED`.

No structural dead end is declared: moving endpoints, nonradial causal networks, optical Jacobi maps and detector-derived readout remain open.
