# Sphere finite-window path-scale landmark

## Exact geometry

On radius-r sphere, K=1/r^2. Consider two transport paths whose enclosed area is protocol family

`A(ell)=eta ell^2`,

with dimensionless shape coefficient eta fixed by loop/window construction. Holonomy angle is

`alpha(ell)=eta ell^2/r^2`.

For equal mixture of `D=diag(lambda1,lambda2)` and transported representative, spectral gap is

`gap(ell)=|lambda1-lambda2| |cos(eta ell^2/r^2)|`.

First isotropy occurs at

`ell_iso=r sqrt(pi/(2 eta))`.

Thus exact connection-derived nonradial scale shape has geometric curvature-radius landmark. Free eta moves reported landmark arbitrarily; physical area coordinate `A` fixes first isotropy at `A_iso=pi r^2/2`. Reparameterizing reported scale or changing loop shape does not create ell0.

## Identifiability

With known eta and first monotone branch, spectral gap identifies `ell/r` only up to orientation/sign lost by absolute cosine. Unknown r and eta are confounded as eta/r^2. Higher branches alias. No ell0 occurs.

## Decision

Classification `KNOWN_RESULT_PLUS_PROJECT_DERIVATION_AND_NEGATIVE_RESULT`; status `SPHERE_WINDOW_NONRADIAL_LANDMARK_CURVATURE_RADIUS_PATH_SHAPE_NOT_ELL0`. Exact 2D geometry, not spacetime UMCH. Structural dead end not met; no detection or reformulation.
