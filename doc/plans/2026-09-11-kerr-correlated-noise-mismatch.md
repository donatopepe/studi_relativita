# Kerr correlated-noise mismatch threshold plan

**Objective:** prove arbitrary shared SPD noise cancellation and derive differential mismatch collision threshold.

**Metric:** `8/8`.

1. Ratify full-noise map, gain domain, spectral norm, exact `tau`, 0.8 bound, threshold witness and J63-J70.
2. Add RED controls and extend authoritative matrix/runner fail-closed.
3. Implement analytic mismatch engine reusing corrected calibration/reference modules.
4. Generate deterministic artifact and theory/EN/IT record.
5. Run focused/full, total/all granular, deterministic, UTF-8, diff, CodeGraph and CI gates; publish only green.

No new tool needed; reuse Kerr runner and shared Hermes UTF-8 validator.
