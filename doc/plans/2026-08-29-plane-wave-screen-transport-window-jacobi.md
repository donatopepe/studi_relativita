# Plane-wave screen-transport/window/Jacobi plan

1. Add failing tests for zero-connection collapse, noncommutative transport/window order, pointwise invariant preservation with insufficient invariant averages, common-basis covariance, affine/profile/connection collision, transport-profile mobility, absent `ell0`, and deterministic artifact.
2. Implement centered screen transport for exact plane-wave `K(u)`, top-hat/triangular windows, raw/transported full Jacobi maps, characteristic coefficients, and scale orbit by composing existing modules.
3. Add bilingual audits and theory with raw objects, equations, classification/status/gate parity, canonical-source limits, declared connection/path/kernel/anchor, and no-dead-end scope.
4. Run focused red/green tests, artifact `--check`, complete suite, numeric audit, and `git diff --check`.
5. Commit, push, open PR, await green tests/LaTeX, auto-merge only if conservative, rerun complete verification on merged `main`, update Hermes, remove worktree and branches.
