# Kerr AR(1) temporal-dependence plan

**Objective:** derive centered finite-N AR(1) covariance penalty and corrected toy count gate.

**Metric:** `8/8`.

1. Ratify separable Gaussian model, exact trace factor, rho/count cases, risk 0.05, J87-J94 and nonclaims.
2. Add RED controls and extend authoritative matrix/runner fail-closed.
3. Implement analytic AR(1) engine reusing estimated-mean covariances and threshold.
4. Generate deterministic artifact and theory/EN/IT record.
5. Run focused/full, total/all granular, deterministic, UTF-8, diff, CodeGraph and CI gates; publish only green.

No new tool needed; reuse Kerr runner and shared Hermes UTF-8 validator.
