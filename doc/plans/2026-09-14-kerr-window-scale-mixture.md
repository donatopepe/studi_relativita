# Kerr window-scale mixture plan

**Objective:** derive exact non-Gaussian covariance risk floor from one latent scale per window.

**Metric:** `8/8`.

1. Ratify scale moments, total-MSE identity, fixed CV/count/floor cases, J159-J166 and nonclaims.
2. Add RED controls and extend authoritative matrix/runner fail-closed.
3. Implement scale-mixture engine reusing latent full-support MSE and branch covariance contrasts.
4. Generate deterministic artifact and theory/EN/IT record.
5. Run focused/full, total/all granular, deterministic, UTF-8, diff, CodeGraph and CI gates; publish only green.

No new tool needed; reuse Kerr runner and shared Hermes UTF-8 validator.
