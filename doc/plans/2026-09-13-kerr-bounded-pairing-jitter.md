# Kerr bounded pairing-jitter plan

**Objective:** derive exact finite-N erosion from uniformly distributed bounded timing offsets.

**Metric:** `8/8`.

1. Ratify uniform integer-jitter mixture, centered trace factor, fixed radii/count cases, J111-J118 and nonclaims.
2. Add RED controls and extend authoritative matrix/runner fail-closed.
3. Implement jitter engine reusing fixed-lag matrices, paired covariances and AR(1) marginal factor.
4. Generate deterministic artifact and theory/EN/IT record.
5. Run focused/full, total/all granular, deterministic, UTF-8, diff, CodeGraph and CI gates; publish only green.

No new tool needed; reuse Kerr runner and shared Hermes UTF-8 validator.
