# Plane-wave common canonical spectrum plan

1. Add failing tests for characteristic coefficients, common-conjugation invariance, reversal blindness, affine canonical similarity, profile sensitivity, and absent `ell0`.
2. Implement pure-Python characteristic polynomial and exact controls reusing full Jacobi utilities; generate deterministic JSON.
3. Add bilingual audits and theory note with raw-map status, nuisance group, source scope, conditional interpretation, and no-dead-end statement.
4. Run focused tests, artifact check, full suite, numeric audit, and `git diff --check`.
5. Commit, push, open PR, wait for tests/LaTeX CI, conservatively merge if green, update Hermes, and remove worktree/branch.
