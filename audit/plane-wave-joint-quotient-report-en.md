# Exact plane-wave joint quotient audit

- Classification: `EXACT_SPACETIME_CROSS_CHANNEL_CONTROL_AND_NEGATIVE_IDENTIFIABILITY_RESULT`.
- Status: `EXACT_PLANE_WAVE_JOINT_QUOTIENT_REVERSAL_AND_AFFINE_SCALE_NONIDENTIFIABLE_NOT_ELL0`.
- Shared exact control: centered affine interval, parallel Brinkmann screen, same smooth symmetric plane-wave `K(u)`, vertex Jacobi boundary.
- Reversal identity: `W_rev=W` and `B_rev=B^T`.
- Raw scope: generic asymmetric profile gives `raw B_rev != raw B`; raw order information exists before quotient.
- Quotient result: eigenvalues of `W` and singular values, determinant and Frobenius norm of `B` are reversal-blind; independent endpoint frames identify `B` with `B^T`.
- Scale result: `L2=sL1`, `K2(u)=K1(u/s)/s^2` preserves spectra of `LW` and singular values of `B/L`.
- Open route: `COMMON_ENDPOINT_ANCHOR_REMAINS_OPEN`; it requires physical anchors, handedness, path transport and calibration.
- Scope: minimal individual endpoint-frame quotient only. It does not exclude every anchored joint cross-invariant.
- `ell0` is absent. This result is not a structural dead end, mechanism, data result, or evidence.
- Conclusion: `NO_POSITIVE_DETECTION_CLAIM`.
