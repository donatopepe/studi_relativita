# Sphere harmonic cross-channel audit

- Classification: `PROJECT_DERIVATION_AND_NEGATIVE_RESULT`
- Status: `SPHERE_CROSS_CHANNEL_HARMONIC_ALGEBRAIC_DEPENDENCE_NOT_ELL0`
- Detection: `NO_POSITIVE_DETECTION_CLAIM`

For exact sphere phase, channels `cos(alpha)` and `cos(2alpha)` obey `y2=2y1^2-1`; joint map has rank at most one and preserves sign/winding aliases. Pair `(|cos alpha|,cos 2alpha)` is also redundant and pi-periodic. More channels add no information when algebraically dependent after projection. Synthetic channels only; `ell0` absent.
