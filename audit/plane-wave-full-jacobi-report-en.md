# Exact plane-wave full Jacobi-map audit — English

## Ledger

- Classification: `EXACT_SPACETIME_CROSS_CHANNEL_CONTROL_AND_NEGATIVE_IDENTIFIABILITY_RESULT`.
- Status: `EXACT_PLANE_WAVE_FULL_JACOBI_LABELLED_ENDPOINT_ORDER_CONDITIONAL_SWAP_AND_AFFINE_SCALE_NONIDENTIFIABLE_NOT_ELL0`.
- Open gate: `PHYSICAL_ENDPOINT_LABELS_AND_CALIBRATION_NOT_DERIVED`.
- UMCH remains `UNPROVEN`.
- Conclusion: `NO_POSITIVE_DETECTION_CLAIM`.

## Counterexample-first result

The exact control retains raw phase-space blocks `A`, `B`, `C`, `D` and verifies symplecticity. For the reversed symmetric optical profile,

`P_rev=E P^T E`,

which gives `A_rev=D^T`, `B_rev=B^T`, `C_rev=C^T`, and `D_rev=A^T`.

When `B` is invertible, source and observer optical matrices `B^{-1}A` and `DB^{-1}` have generically different spectra. Reversal swaps those spectra. Thus derivative blocks preserve profile orientation only with prospectively fixed physical source/observer labels, affine normalization, screen transport, and endpoint calibration.

If endpoint swap belongs to the nuisance quotient, `P` and `P_rev` are exactly equivalent. Under affine/profile rescaling, `A` and `D` are unchanged, `B` scales by `s`, and `C` by `1/s`; the dimensionless full map is unchanged. Absolute support scale and `ell0` remain unidentified.

## Scope

Coley–McNutt–Milson (2012), DOI `10.1088/0264-9381/29/23/235023`, supports exact vacuum Brinkmann plane waves and curvature-driven geodesic deviation. Full-map protocol, selected boundary, endpoint labels, endpoint swap, detector calibration, UMCH, `ell0`, and detection are not source-established.

Raw blocks remain primary; endpoint spectra are dependent diagnostics. No physical derivative observable or calibration is derived. Remaining routes include physically labelled endpoints, full Sachs observables, causal windows, transport, and other exact geometries; this is not a structural dead end. No data or detection claim.
