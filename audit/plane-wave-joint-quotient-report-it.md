# Audit del quotient congiunto per onda piana esatta

- Classificazione: `EXACT_SPACETIME_CROSS_CHANNEL_CONTROL_AND_NEGATIVE_IDENTIFIABILITY_RESULT`.
- Stato: `EXACT_PLANE_WAVE_JOINT_QUOTIENT_REVERSAL_AND_AFFINE_SCALE_NONIDENTIFIABLE_NOT_ELL0`.
- Controllo esatto condiviso: intervallo affine centrato, screen Brinkmann parallelo, stesso `K(u)` liscio e simmetrico di onda piana, boundary Jacobi vertex.
- Identità di reversal: `W_rev=W` e `B_rev=B^T`.
- Scope raw: un profilo asimmetrico generico dà `raw B_rev != raw B`; informazione d'ordine raw esiste prima del quotient.
- Risultato del quotient: autovalori di `W` e valori singolari, determinante e norma di Frobenius di `B` sono reversal-blind; frame endpoint indipendenti identificano `B` con `B^T`.
- Risultato di scala: `L2=sL1`, `K2(u)=K1(u/s)/s^2` preserva gli spettri di `LW` e i valori singolari di `B/L`.
- Route aperta: `COMMON_ENDPOINT_ANCHOR_REMAINS_OPEN`; richiede anchor fisici, handedness, path transport e calibrazione.
- Scope: solo quotient individuale minimo dei frame endpoint. Non esclude ogni cross-invariant con anchor congiunto.
- `ell0` è assente. Questo risultato non è un vicolo cieco strutturale, un meccanismo, un risultato dati o evidenza.
- Conclusione: `NO_POSITIVE_DETECTION_CLAIM`.
