# Exact plane-wave common-anchor audit — English

## Ledger

- Classification: `EXACT_SPACETIME_CROSS_CHANNEL_CONTROL_AND_NEGATIVE_IDENTIFIABILITY_RESULT`.
- Status: `EXACT_PLANE_WAVE_COMMON_ORIENTED_ANCHOR_RECOVERS_REVERSAL_SIGN_CONDITIONALLY_NOT_ELL0`.
- Open gate: `PHYSICAL_ORIENTED_ENDPOINT_ANCHOR_NOT_DERIVED`.
- UMCH remains `UNPROVEN`.
- Conclusion: `NO_POSITIVE_DETECTION_CLAIM`.

## Counterexample-first result

For the same exact plane-wave `K(u)`, centered interval, parallel screen, and vertex boundary as prior gates, reversal gives `W_rev=W` and `B_rev=B^T`. Define `a(B)=(B21-B12)/2`. Generic asymmetric profile yields nonzero `a(B)`.

- Under a physically common oriented anchor, `B -> Q B Q^T` with `Q in SO(2)`: `a(B)` is invariant and reversal flips its sign.
- Under a common but unoriented `O(2)` anchor: reflection flips the sign, so handedness remains ambiguous.
- Under independent endpoints `SO(2) x SO(2)`: `B` and `B^T` remain orbit-equivalent.
- Under affine/profile rescaling: `a(B)/L` is preserved, so absolute support scale remains unidentified.

Therefore common oriented anchoring conditionally recovers profile orientation, but neither derives absolute scale nor introduces `ell0`.

## Scope

Coley–McNutt–Milson (2012), DOI `10.1088/0264-9381/29/23/235023`, supports exact vacuum Brinkmann plane waves and curvature-driven geodesic deviation. Common endpoint anchor, handedness, finite window, vertex boundary, detector calibration, UMCH, `ell0`, and detection are not source-established.

Exact control uses mathematical parallel-screen trivialization; no physical source/observer tetrad identification or parity calibration is derived. Route remains open and this is not a structural dead end. No data or detection claim.
