# Jacobi path-ordering audit

- Classification: `TOY_CONTROL_AND_NEGATIVE_RESULT`
- Status: `JACOBI_PATH_ORDER_REQUIRED_LOCAL_SPECTRA_INSUFFICIENT`
- Detection: `NO_POSITIVE_DETECTION_CLAIM`

Opposite ordering of noncommuting optical-tidal segments yields different endpoint Jacobi maps despite same unordered local spectra and integrated trace. Local spectral summaries are insufficient; ordered history, screen transport, affine normalization and boundary data are required. Ordering sensitivity remains geometric/path-dependent, with `ell0` absent.
